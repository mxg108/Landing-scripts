# Environment
.env
*.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Audio files — never commit real call recordings
*.mp4
*.mp3
*.wav
*.m4a
data/

# ChromaDB local store
chroma_db/

# Audit logs
audit.log
*.jsonl

# OS
.DS_Store
Thumbs.db

#  Google Apps Scripts IDs
.clasp.json
appsscript.json

# Call-time backfill (PR-2 of CallTimeOnAnalystHistory.md) — local audit
# trail for the backfill script. Never commit; rerun locally per team.
.backfill-log
.backfill-skipped.csv
# Stage-A annotation smoke outputs — verbatim caller speech (PII), never commit
scripts/annotations/
# Pulpo compendium snapshots — full internal KB contents, never commit
scripts/.compendium/
scripts/shadow_sync.log
